# Deploy on Railway: service with Root Directory = backend/
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "python pupu_bot.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
